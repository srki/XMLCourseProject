curl -v -X POST  --anyauth -u root:root \
  --header "Content-Type:application/json" \
  -d '{"user-name":"rest-writer", "password": "writer", "role": ["rest-writer"]}' \
  http://localhost:8002/manage/v2/users
